import { theThing } from ".";

const testInput = ``;
const expected = ``;

describe("first test", () => {
  it("should pass", () => {
    const given = theThing(testInput);

    expect(given).toBe(expected);
  });
});
